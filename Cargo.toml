[package]
name = "blockchain_simulator"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
md5 = "0.7"  # Adding the md5 hashing crate (used in Block)

